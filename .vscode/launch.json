{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run live serial manual batch",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/run.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
